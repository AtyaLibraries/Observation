name: CI

on:
  push:
    branches: [development, master]
  pull_request:
    branches: [development, master]
  workflow_dispatch:

permissions:
  contents: read
  checks: write

jobs:
  ci:
    uses: AtyaLibraries/github-workflows/.github/workflows/dotnet-package-ci.yml@v1
    with:
      solution: ./Observation.sln
      test-project: ./tests/Observation.UnitTests/Observation.UnitTests.csproj
      package-project: ./src/Observation/Observation.csproj
